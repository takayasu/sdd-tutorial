# Section 1: 在庫ロットのライフサイクル

在庫ロット（最小管理単位）の状態遷移を型で表現する。

## ドメインモデル DSL

```
// ============================================================
// Section 1: 在庫ロット
// ============================================================

// ----------------------------------------------------------------
// 基本値型
// ----------------------------------------------------------------

data 事業部コード = 整数
data 部門コード = 整数
data 担当課コード = 整数

// ロット番号（年度・保管場所・連番の組み合わせで一意）
data ロット番号 = ロット番号年度 AND ロット番号保管場所 AND ロット番号連番

data 金額 = 整数       // 0以上、円単位
data 数量 = 数値       // 0.001以上
data 個数 = 整数       // 1以上

// ----------------------------------------------------------------
// ロット明細（在庫ロットの構成要素）
// ----------------------------------------------------------------

data ロット明細 = 品目区分
    AND 上位品区分?
    AND 商品分類コード
    AND 長さ規格下限
    AND 太さ規格下限
    AND 太さ規格上限
    AND 品質等級
    AND 個数
    AND 数量
    AND 検査結果区分?

data 品目区分 = 一般品 OR 上位品 OR 特注品

// ----------------------------------------------------------------
// 在庫ロット（最小管理単位）
// ----------------------------------------------------------------

data 在庫ロット = 製造中ロット OR 製造完了ロット OR 出荷指示済みロット OR 出荷完了ロット
// 注: Section 3で「変換指示済みロット」が追加される（品目変換機能）

data ロット共通 = ロット番号
    AND 事業部コード
    AND 部門コード
    AND 担当課コード
    AND 工程区分
    AND 検査区分
    AND 製造区分
    AND List<ロット明細> // 1件以上

data 工程区分 = 整数
data 検査区分 = 整数
data 製造区分 = 整数

data 製造中ロット = ロット共通
data 製造完了ロット = ロット共通 AND 製造完了日
data 出荷指示済みロット = ロット共通 AND 製造完了日 AND 出荷期限日
data 出荷完了ロット = ロット共通 AND 製造完了日 AND 出荷期限日 AND 出荷日

// ----------------------------------------------------------------
// 振る舞い
// ----------------------------------------------------------------

behavior 製造完了を指示する = 製造中ロット AND 製造完了日 -> 製造完了ロット OR 製造完了指示エラー
behavior 出荷を指示する = 製造完了ロット AND 出荷期限日 -> 出荷指示済みロット OR 出荷指示エラー
behavior 出荷完了を指示する = 出荷指示済みロット AND 出荷日 -> 出荷完了ロット OR 出荷完了指示エラー
behavior 製造完了を取り消す = 製造完了ロット -> 製造中ロット OR 取消エラー
```

## 学習ポイント

- 状態をフラグではなくORで分離し、各状態に必要なデータだけを持たせる
- 不正な状態遷移（例：製造中ロットに出荷完了を指示する）が型レベルでコンパイルエラーになる
- behaviorの入力型と出力型が明示されているため、関数シグネチャがそのまま決まる
